<li>
	🍔{{burger_name}}

	<button class="devour" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}EATEN!{{/if}}
	</button>

	
</li>


{{!-- <li class="collection-item">
	🍔 {{burger_name}}
{{#if devoured}}
	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		Eat The Burger!
	</button>
{{/if}}
</li> --}}